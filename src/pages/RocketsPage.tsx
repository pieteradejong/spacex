import { FC } from 'react';
import { useQuery } from 'react-query'

const RocketsPage: FC = () => {
    return (
        <div>
            Rockets Page
        </div>
    )
}

export default RocketsPage;
