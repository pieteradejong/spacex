export type Crew = {
    id: string;
}

export type LaunchApiResult = Launch[];

export type Launch = {
    id: string;
    name: string;
    rocket: string;
};

export type Rocket = {
    id: string;
}

